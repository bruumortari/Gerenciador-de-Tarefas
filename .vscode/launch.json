{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Java Application",
            "request": "launch",
            "mainClass": "TaskManagerGUI",  // Nome da classe principal
            "classPaths": [
                "bin",  // Diretório onde os arquivos .class são armazenados
                "lib/mysql-connector-j-9.0.0.jar"  // Caminho para o JAR do driver JDBC
            ]
        }
    ]
}
